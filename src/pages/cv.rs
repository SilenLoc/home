use yew::prelude::*;
use yew_router::prelude::*;

use crate::Route;

#[function_component]
pub fn CV() -> Html {
        html! {
        <div class="section container">
            <h1 class="title">{ "CV" }</h1>
            <h2 class="subtitle">{ "c" }</h2>
        </div>
    }

}

